# Core architecture

The core layer contains detector-independent domain models and recommendation logic.
UI, Android bindings, camera analysis, audio analysis, GPS and persistence will be added in separate layers.
